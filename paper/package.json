{
  "name": "paper",
  "version": "1.0.0",
  "description": "IEEE conference format (`IEEEtran`). Every number in the Results section comes\r from the committed run artifacts under `results/`; every figure is regenerated\r from those same files. Nothing is hand-entered.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "docx": "^9.7.1",
    "jszip": "^3.10.1"
  }
}
